<UserControl x:Class="Atualizador.Views.EmpresasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- filtros principais -->
        <Border Grid.Row="0" Padding="8" Margin="0,0,0,8" Background="#FFF" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" DockPanel.Dock="Top">
                    <TextBlock Text="Buscar (nome):" VerticalAlignment="Center" Margin="4" FontWeight="SemiBold" />
                    <TextBox x:Name="TbSearch" Width="240" Margin="4" VerticalAlignment="Center" ToolTip="Pesquisar por nome" FontSize="13" Height="26" />

                    <TextBlock Text="Empresa:" VerticalAlignment="Center" Margin="12,4,4,4" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CbEmpresa" Width="200" Margin="4" Height="26" />

                    <TextBlock Text="Regime:" VerticalAlignment="Center" Margin="12,4,4,4" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CbRegime" Width="200" Margin="4" Height="26" />

                    <TextBlock Text="UF:" VerticalAlignment="Center" Margin="12,4,4,4" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CbUf" Width="80" Margin="4" Height="26" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,8,0,0">
                    <TextBlock Text="Período:" VerticalAlignment="Center" Margin="4" FontWeight="SemiBold"/>
                    <DatePicker x:Name="DpFrom" Width="140" Margin="4" />
                    <TextBlock Text="a" VerticalAlignment="Center" Margin="4"/> 
                    <DatePicker x:Name="DpTo" Width="140" Margin="4" />

                    <Button x:Name="BtnAplicarFiltros" Content="Aplicar" Margin="12,4,4,4" Padding="8,4" Click="BtnAplicarFiltros_Click"/>
                    <Button x:Name="BtnLimparFiltros" Content="Limpar" Margin="4" Padding="8,4" Click="BtnLimparFiltros_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <DataGrid x:Name="GridClientes" AutoGenerateColumns="False" Grid.Row="1" SelectionChanged="GridClientes_SelectionChanged" CanUserSortColumns="True" Sorting="GridClientes_Sorting" AlternatingRowBackground="#F7F7F7" RowBackground="White" GridLinesVisibility="None" Margin="0,4,0,4" CanUserAddRows="False" RowHeight="28">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" SortMemberPath="Codigo" FontSize="14" Width="80"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" SortMemberPath="Nome" FontSize="14" Width="300" />

                <DataGridTextColumn Header="CNPJ" Binding="{Binding Cnpj}" SortMemberPath="Cnpj" Width="160" />
                <DataGridTextColumn Header="UF" Binding="{Binding Uf}" SortMemberPath="Uf" Width="60" />
                <DataGridTextColumn Header="Comunicacao" Binding="{Binding Comunicacao}" SortMemberPath="Comunicacao" Width="90" />

                <DataGridCheckBoxColumn Header="Admin" Binding="{Binding Admin}" SortMemberPath="Admin" />
                <DataGridCheckBoxColumn Header="Caixa" Binding="{Binding Caixa}" SortMemberPath="Caixa" />
                <DataGridCheckBoxColumn Header="Copy" Binding="{Binding Copy}" SortMemberPath="Copy" />
                <DataGridCheckBoxColumn Header="Conect" Binding="{Binding Conect}" SortMemberPath="Conect" />
                <DataGridTextColumn Header="Regime" Binding="{Binding Regime_Tributario}" SortMemberPath="Regime_Tributario" Width="160" />
                <DataGridTextColumn Header="Data Atualização" Binding="{Binding DataAtualizacao, StringFormat=d}" SortMemberPath="DataAtualizacao" Width="140" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" DockPanel.Dock="Left">
                <Button x:Name="BtnPrev" Content="◀ Anterior" Height="32" Width="100" Margin="0,0,8,0" Click="BtnPrev_Click"/>
                <TextBlock x:Name="TbPagina" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Button x:Name="BtnNext" Content="Próximo ▶" Height="32" Width="100" Click="BtnNext_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="Excluir" Height="36" Width="120" Margin="0,0,8,0" Click="BtnExcluir_Click" FontSize="14" />
                <Button Content="Atualizar" Height="36" Width="140" Click="BtnAtualizar_Click" FontSize="14" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>